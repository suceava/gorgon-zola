{
  "name": "cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npx cdk deploy --all --require-approval never",
    "synth": "npx cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.27.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
